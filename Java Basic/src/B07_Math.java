
public class B07_Math {

	public static void main(String[] args) {
		
		int max = Math.max(5,  10); // return the highest value
		int min = Math.min(5,  10); // return the lowest value
		double sqrt = Math.sqrt(16); // returns the square root
		
		double abs = Math.abs(-4.7); // returns the absolute (positive) value
		double ran = Math.random(); // returns a random number between 0.0 (inclusive), and 1.0(exclusive)
		
	}
}
